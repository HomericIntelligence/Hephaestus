{% if command_prompt | default(false) %}{{ prompt }}

{% endif %}
You are running Hephaestus agent stage `{{ stage }}`.

{% if skill_text %}
Use these skill instructions as authoritative context for this stage:

{{ skill_text }}

---

{% endif %}
{% if not (command_prompt | default(false)) %}{{ prompt -}}{% endif -%}
