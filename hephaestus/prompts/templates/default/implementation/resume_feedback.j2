
The independent reviewer for issue #{{ issue_number }} produced the following
feedback on iteration {{ prev_iteration }}:

---

{{ review_feedback_block }}

---

Address every concrete finding above. Update the code (and tests, if needed)
in this same working directory. Do NOT commit or push — those phases run
after the review loop terminates.

When done, summarize what you changed in 3-5 bullet points so the next
review can verify the fixes were applied. Every supplied review thread is
required: investigate and fix each one, then emit exactly one fenced JSON
object containing every supplied thread ID and a concise reply for each. Do
not omit, dismiss, or defer a thread; partial output is rejected:

```json
{"addressed": ["<thread_id>"], "replies": {"<thread_id>": "what was fixed"}}
```

Do not resolve GitHub threads. The coordinator posts an implementation reply
only after the fix commit is pushed; a fresh reviewer validates and resolves
or returns each thread.

{{ untrusted_notice }}

{{ terse_output_directive }}
