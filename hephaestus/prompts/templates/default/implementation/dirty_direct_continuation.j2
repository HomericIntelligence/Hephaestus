Continue the approved implementation in the current worktree. Preserve the existing changes.
You have one edit-and-test turn. Change only the exact allowed paths. Run the required tests.
Do not stage files, create commits, push branches, or create branch reservations.
Do not write to GitHub. The host owns staging, signed commits, publication, and PR creation.
Do not reset, discard, or stash the existing work. If you cannot complete the plan, report the blocker.

{{ untrusted_notice }}

{{ plan_block }}

{{ review_block }}

{{ paths_block }}

{{ status_block }}

{{ diff_block }}
